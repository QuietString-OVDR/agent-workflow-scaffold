# Agent Work Area

This directory is the working area for generated artifacts, scratch files, downloads, logs, and temporary outputs.

Rules:

- Do not store canonical docs or tracked design artifacts here
- Move any result that should stay in the repository to an appropriate tracked path
- By default, the contents of this directory are excluded through `.gitignore`
