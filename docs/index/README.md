# Branch Docs Index

This directory stores local indexes used by branch-docs-starter.

- `branch-bindings.json`: maps raw Git branch names to canonical Jira work keys.
- `work-items.json`: caches Jira parent/child context for local agent sessions.

These files are local agent context in target work repositories and are intentionally ignored by Git.
